#
# https://help.github.com/articles/dealing-with-line-endings/
#
# Auto detect text files and perform LF normalization
# Enforce LF line endings regardless of OS or git configs
* text=auto eol=lf

# Windows CMD & Visual Studio solution
*.bat text eol=crlf
*.cmd text eol=crlf
*.sln text eol=crlf
